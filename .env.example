# MongoDB Configuration
# Choose one of the following MongoDB connection options:

# Option 1: MongoDB URI (recommended)
MONGODB_URI=mongodb://localhost:27017/bagcalculator

# Option 2: Alternative using DATABASE_URL (if MONGODB_URI is not set)
# DATABASE_URL=mongodb://localhost:27017/bagcalculator

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bagcalculator?retryWrites=true&w=majority

# For MongoDB with authentication:
# MONGODB_URI=mongodb://username:password@localhost:27017/bagcalculator

# Application Configuration
NODE_ENV=development
PORT=5000

# Session Configuration (if using sessions)
SESSION_SECRET=your-secret-key-here

# QuickBase API Configuration (for inventory data)
# QUICKBASE_HOSTNAME=your-quickbase-hostname
# QUICKBASE_USER_TOKEN=your-user-token
# QUICKBASE_APP_ID=your-app-id

# Optional: Enable debug logging
# DEBUG=mongoose:*

# Example Production Configuration:
# NODE_ENV=production
# MONGODB_URI=mongodb+srv://prod-user:secure-password@production-cluster.mongodb.net/bagcalculator-prod?retryWrites=true&w=majority
# SESSION_SECRET=very-secure-random-string-for-production
